services:
  frontend:
    image: ghcr.io/majedal01/homelab/ynab_insights_frontend:prod-latest
    pull_policy: always
    ports:
      - "8002:3000"
    environment:
      NODE_ENV: production
      BACKEND_URL: http://app:8000
    depends_on:
      - app
    restart: unless-stopped

  app:
    image: ghcr.io/majedal01/homelab/ynab_insights:prod-latest
    pull_policy: always
    # No host port mapping — only the frontend talks to FastAPI, over the
    # compose-internal network at http://app:8000.
    environment:
      APP_ENV: prod
      APP_VERSION: ${APP_VERSION:-unknown}
      POSTGRES_HOST: db
      POSTGRES_PORT: "5432"
      POSTGRES_USER: ${POSTGRES_USER:?POSTGRES_USER is required}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      POSTGRES_DB: ${POSTGRES_DB:?POSTGRES_DB is required}
      YNAB_TOKEN: ${YNAB_TOKEN:-}
      YNAB_BUDGET_ID: ${YNAB_BUDGET_ID:-}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
    depends_on:
      - db
    restart: unless-stopped

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER:?POSTGRES_USER is required}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      POSTGRES_DB: ${POSTGRES_DB:?POSTGRES_DB is required}
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  pgdata:
