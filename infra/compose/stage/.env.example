POSTGRES_USER=changeme
POSTGRES_PASSWORD=changeme
POSTGRES_DB=changeme

# APP_VERSION is set by the deploy workflow per deploy (commit SHA).
# Uncomment and set only if bringing the stack up manually on the VM.
# APP_VERSION=manual
