POSTGRES_USER=dev
POSTGRES_PASSWORD=dev
POSTGRES_DB=dev

# Optional. /sync returns 503 until YNAB_TOKEN is set; /ask returns 503 until
# ANTHROPIC_API_KEY is set. The rest of the app works without them.
YNAB_TOKEN=
YNAB_BUDGET_ID=
ANTHROPIC_API_KEY=
